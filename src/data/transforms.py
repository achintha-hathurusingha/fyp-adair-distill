"""Paired augmentation (crop / flip / rotate). Implemented in Task 2."""
from __future__ import annotations

_TASK = "Task 2 — data pipeline"


def paired_random_crop(*_args, **_kwargs):
    """Placeholder — implemented in Task 2."""
    raise NotImplementedError(f"transforms.paired_random_crop is scaffolded for {_TASK}.")

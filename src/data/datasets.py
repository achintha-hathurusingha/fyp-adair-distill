"""Datasets for the 3-degradation protocol (BSD400/WED, Rain100L, RESIDE).

Implemented in Task 2. Stubbed now so structure and imports are stable.
"""
from __future__ import annotations

_TASK = "Task 2 — data pipeline"


def build_dataset(*_args, **_kwargs):
    """Placeholder — implemented in Task 2."""
    raise NotImplementedError(f"datasets.build_dataset is scaffolded for {_TASK}.")

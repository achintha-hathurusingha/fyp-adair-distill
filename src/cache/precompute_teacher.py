"""Cache frozen-AdaIR outputs over the training set — implemented in Task 4.

Requirements: lossless storage (uint8 PNG / lossless WebP), a manifest mapping
input -> output, resumable execution, and integrity verification on reload.
"""
from __future__ import annotations

_TASK = "Task 4 — teacher output caching"

if __name__ == "__main__":
    raise SystemExit(f"src/cache/precompute_teacher.py is scaffolded for {_TASK}.")

"""Training CLI entry point — implemented in Task 5.

Will take ``--config``, ``--seed``, ``--resume`` and write a full run directory.
"""
from __future__ import annotations

_TASK = "Task 5 — B0 baseline"

if __name__ == "__main__":
    raise SystemExit(f"src/train/train.py is scaffolded for {_TASK}.")

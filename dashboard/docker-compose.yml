services:
  kd-freq-dashboard:
    build: .
    container_name: kd-freq-dashboard
    ports:
      - "8080:8080"
    volumes:
      - /home/minura/fyp-adair-distill/runs:/data/runs:ro
      - /tmp/kd_freq_3seed.log:/data/logs/kd_freq_3seed.log:ro
    restart: unless-stopped
